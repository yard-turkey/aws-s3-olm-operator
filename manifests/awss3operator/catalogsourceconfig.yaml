apiVersion: "operators.coreos.com/v1"
kind: "CatalogSourceConfig"
metadata:
  name: "community-operator-provisioners"
  namespace: "openshift-marketplace"
spec:
  targetNamespace: s3-provisioner
  packages: awss3operator
